extends base

block head
  title= post.title
  if post.description
    meta(name="description", content="#{post.description}")
  base(href="http://gu.illau.me#{data.baseUrl}#{post.url}", target="_top")

block content
  mixin post(post)
  include _social
